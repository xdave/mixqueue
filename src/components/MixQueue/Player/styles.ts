import theme from '../../../util/theme';

export const styles = {
    paper: {
        position: 'relative',
        margin: `${theme.spacing.unit}px`,
        padding: `${theme.spacing.unit}px`
    },
    tracklist: {
        height: '175px',
        overflowY: 'scroll'
    },
    track: {
        fontWeight: 'bold'
    }
}
